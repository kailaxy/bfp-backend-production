[phases.setup]
nixPkgs = ["nodejs_20", "python311"]

[phases.install]
cmds = ["npm ci"]

[phases.postInstall]
cmds = ["python3 -m pip install --upgrade pip", "python3 -m pip install -r forecasting/requirements.txt"]

[start]
cmd = "node server.js"

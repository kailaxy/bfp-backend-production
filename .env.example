# Copy this file to `.env` and fill in real values. Do NOT commit `.env`.
# Example backend environment variables for local development
PORT=5000

# Local database (for local development)
DB_USER=postgres
# DB_PASSWORD should be set to your local database password
DB_PASSWORD=your_db_password_here
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bfpmapping

# Production database URL from Render (for forecast generation)
# Get this from: Render Dashboard → bfp-backend → Environment → DATABASE_URL (Internal)
# Format: postgresql://user:password@host:port/database
# Example: postgresql://bfp_backend_user:pass@dpg-xxxxx.oregon-postgres.render.com/bfp_backend
PRODUCTION_DATABASE_URL=

# Google API key used server-side if needed
GOOGLE_API_KEY=your_google_api_key_here

# JWT Secret
JWT_SECRET=your_jwt_secret_here
